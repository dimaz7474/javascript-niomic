// Mendeklarasikan variabel dengan var
var message = "Halo, selamat datang di website kami!";

// Menampilkan pesan di console
console.log(message);

// Menampilkan pesan di alert box
alert(message);

// Menampilkan pesan ke dalam elemen HTML (jika ada elemen dengan id 'output')
document.getElementById("output").innerText = message;
